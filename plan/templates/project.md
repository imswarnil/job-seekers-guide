---
title: Replace with the project step title
description: One sentence for a stranger. Name the piece of the app this adds.
minutes: 30
kind: project
---

<!--
  Kind: project, spec tag [A]. Advances the University Management App, the ONE
  system the whole course builds (specified in content-plan.md, Part D). Give
  the PLAN and the ACCEPTANCE CRITERIA (files, functions, routes, columns,
  what "done" looks like), NOT the finished code. The reader builds it. The
  only exceptions are the assembly lesson at the end of a track and the
  finished-system lesson at the very end.
  Before writing: restate to yourself what already exists in the app and do
  not contradict it.
  Beat 1 goes right here, with no heading: resolve the previous cliffhanger,
  then say which piece of the app today adds.
  Reminder: the em dash is banned in prose. Full stop, comma, colon or brackets.
-->

## Why this matters in a job

<!--
  This is the portfolio piece an interviewer will actually open. Say what a
  hiring manager looks for in exactly this kind of feature. A ::real-life
  block fits here.
-->

## What you are building today

<!--
  The requirement, in plain words, as the registrar or dean would state it.
  Then the plan: which files, which functions, which routes, which columns.
  ::flow for how the pieces connect, ::steps for the build order,
  ::feature-list for the moving parts. No finished code.
-->

## Acceptance criteria

<!--
  What "done" looks like, checkable one line at a time: the command to run,
  the URL to open, the rows that come back, the exact output. ::field or a
  table works well. Every criterion is something the reader can verify in the
  terminal or the browser, not a feeling.
-->

## Hints, if you are stuck

<!--
  Hints only, behind an ::accordion, ordered from a nudge to a shove. The last
  hint may name the function signature; none of them paste the body. Warn
  before any step that can destroy data, BEFORE the command appears.
-->

::accordion
  :::accordion-item{label="Hint 1: a nudge"}
  <!-- The smallest possible pointer. -->
  :::
::

## Check yourself

<!--
  Exactly two questions. One is the kind an interviewer asks about a project:
  "walk me through how you built this" or "why did you structure it that way".
-->

## The interview question this answers

<!--
  Project lessons feed the "tell me about something you built" question.
  Phrase it as asked, then what a good answer contains, in an ::accordion.
-->

## Next

<!--
  The cliffhanger, last thing on the page. Replace the example with the mapped
  hook from content-plan.md.
-->

::callout{icon="i-lucide-arrow-right"}
The applications table renders, but it shows every applicant to anybody who
opens the page. The registrar can see it, and so can the applicants' rivals.
Somebody has to teach the app who is allowed to see what.
::
