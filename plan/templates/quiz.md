---
title: Replace with the interview Q&A title
description: One sentence for a stranger. Say which chapter this interrogates.
minutes: 12
kind: quiz
---

<!--
  Kind: quiz, spec tag [Q]. Interview. Every answer is PROVED with code, a
  query or a diagram, never only defined. The framing is always reassurance:
  the reader already wrote this a few lessons ago; here is how to say it out
  loud in ten seconds.
  Beat 1 goes right here, with no heading: resolve the previous cliffhanger,
  then one line on why these questions and why now.
  Reminder: the em dash is banned in prose. Full stop, comma, colon or brackets.
-->

## Why this matters in a job

<!--
  These are the questions a phone screen opens with. Name where in the process
  they appear (screening call, take-home, whiteboard) and what the interviewer
  is actually listening for.
-->

## The questions

<!--
  One ::accordion, one item per question. The label is the question phrased
  the way it is REALLY asked, informal and slightly ambiguous, the way a real
  interviewer says it. Inside: a short, confident answer a nervous person can
  say in ten seconds, then the proof (a runnable snippet, a query with its
  rows, or a diagram). For data and JavaScript, prefer "predict the result of
  this" questions.
-->

::accordion
  :::accordion-item{label="So, what actually happens when you run this?"}
  <!-- The ten-second answer, then the code that proves it, with its output. -->
  :::
::

## Check yourself

<!--
  Exactly two questions, and here both can be interviewer-style. Make at least
  one a "predict the result" if the subject allows it.
-->

## Next

<!--
  The cliffhanger, last thing on the page. A quiz file still obeys the chain:
  it resolves the hook before it and sets the hook after it. Replace the
  example with the mapped hook from content-plan.md.
-->

::callout{icon="i-lucide-arrow-right"}
You can answer every question this chapter produces. What you cannot yet do is
the thing the next interviewer will ask for first: show them a query that
joins two tables. That is the whole next chapter.
::
