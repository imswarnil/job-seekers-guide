# Local dev only: other localhost projects (Supabase, PostHog, …) share the
# localhost cookie jar and can push the Cookie header past Apache's 8KB
# default, causing 400 Bad Request. Raise the per-field limit.
LimitRequestFieldSize 32768
