# Public URL, used for OG images, canonicals and the sitemap when running
# `nuxt generate`. Without it those come out relative and social previews break.
NUXT_PUBLIC_SITE_URL=

# Nuxt Studio's AI assistant. Optional — Studio edits content without it; this
# only enables the assistant that reads .studio/ for context.
AI_GATEWAY_API_KEY=
