# SEO / Structured Data

Planned: JSON-LD `Course` schema on course pages, `BreadcrumbList` sitewide,
`Organization` on the homepage, per-template meta tags, and sitemap hooks for
the custom post types. All generated from real post data — no placeholder
fields.

See TODO.md section 6.
